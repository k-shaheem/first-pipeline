name: My First CI Pipeline
on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Using a newer version to avoid those warnings!

      - name: Install dependencies
        run: echo "In a real project, I would run 'npm install' here"

      - name: Run Build
        run: |
          echo "Building the project..."
          echo "Build successful!"
